# This is generated locally by `chezmoi init`. Keep the working copy selected
# during initialization so subsequent chezmoi commands need no `--source` flag.
sourceDir = {{ printf "%q" .chezmoi.sourceDir }}
